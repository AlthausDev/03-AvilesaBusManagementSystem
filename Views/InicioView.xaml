<Page x:Class="Project._04_LineasAutobuses.Views.InicioView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:Project._04_LineasAutobuses.Views"
      Title="InicioView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Etiquetas y controles para la consulta de itinerarios -->
        <Label Grid.Row="0" Content="Municipio de Origen:"/>
        <TextBox Grid.Row="0" Margin="150,0,0,0" Name="textBoxMunicipioOrigen"/>

        <Label Grid.Row="1" Content="Municipio de Destino:"/>
        <TextBox Grid.Row="1" Margin="150,0,0,0" Name="textBoxMunicipioDestino"/>

        <Label Grid.Row="2" Content="Hora Seleccionada:"/>
        <!--<controls:TimePickerControl Grid.Row="2" Margin="150,0,0,0" Name="customTimePicker"/>-->

        <Button Content="Buscar" Command="{Binding BuscarCommand}" />

        <!-- Frame para cargar la página de ItinerariosView -->
        <Frame Grid.Row="4" Name="mainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
